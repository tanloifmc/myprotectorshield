{
  "name": "myprotector-shield-website",
  "version": "1.0.0",
  "private": true,
  
"scripts": {
  "build": "pnpm run build:landing && pnpm run build:app",
  "build:landing": "cd myprotector-landing && pnpm install && pnpm run build && cp -r dist/* ../dist/",
  "build:app": "cd myprotector-shield && pnpm install && pnpm run build && mkdir -p ../dist/app && cp -r dist/* ../dist/app/"
},
  
"devDependencies": {
    "concurrently": "^8.2.0"
  }
}
