{
  "name": "myprotector-shield-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:landing && npm run build:app",
    "build:landing": "cd myprotector-landing && npm install && npm run build && cp -r dist/* ../dist/",
    "build:app": "cd myprotector-shield && npm install && npm run build && mkdir -p ../dist/app && cp -r dist/* ../dist/app/"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
